#include <stdio.h>
/**
 * main - Entry point
 *
 * Return:
 * Always 0 (Success)
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			putcher(c);
			c++;
		}
	putcher('\n');
	return (0);
}
